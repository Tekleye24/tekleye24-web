# Copy this file to .env.local and fill in your real Stripe secret key.
# .env.local is gitignored — never commit real keys.

# From https://dashboard.stripe.com/apikeys (use the "Secret key")
STRIPE_SECRET_KEY=sk_test_...
